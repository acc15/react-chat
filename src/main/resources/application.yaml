spring:
  jackson:
    serialization:
      WRITE_DATE_TIMESTAMPS_AS_NANOSECONDS: false

server:
  port: ${PORT:8080}
#  ssl:
#    key-store: classpath:keystore.jks
#    key-store-password: pwd1
#    key-password: pwd1
#    key-alias: noname